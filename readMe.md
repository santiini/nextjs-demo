# Nextjs 从零开始

## 项目构建

### 依赖安装

```bash
mkdir TAN
cd TAN
npm init -y

npm install react react-dom express next --save
npm install @types/{react,react-dom} --save-dev
```

### 项目启动

```bash
{
  ...
  "scripts": {
    "dev": "next"
  }
  ...
}
```
